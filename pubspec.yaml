name: mcp_profile
description: AI persona definitions with template rendering, context injection, profile selection, and appraisal scoring for the MCP ecosystem.
version: 0.2.1
homepage: https://app-appplayer.github.io/makemind
repository: https://github.com/app-appplayer/mcp_profile
issue_tracker: https://github.com/app-appplayer/mcp_profile/issues

topics:
  - ai-personas
  - mcp
  - ai
  - dart
  - templates

environment:
  sdk: ^3.0.0

dependencies:
  meta: ^1.9.0
  mcp_bundle: ^0.4.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
  mockito: ^5.4.0
  build_runner: ^2.4.0

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:
